export type ReviewProps = {
    review: {
        header: string,
        description: {
            image: string,
            fullname: string,
            city: string,
            date: string,
            description: string
        }
    }
}